import lib.process as process

pub func get(name: ptr) -> ptr:
  return process.env(name)

pub func argc() -> i32:
  return process.argc()

pub func argv(index: i32) -> ptr:
  return process.argv(index)
